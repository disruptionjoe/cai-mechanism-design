# Preserve a dominated synthetic alternative without forced cardinality

## Boundary

Write one complete synthetic Mechanism Design process fixture from only the
embedded evidence. Preserve no action, the pointer-only correction route, and
the supplied dominated mirror as visible negative evidence. Do not manufacture
another alternative, flatten the dominated route into the preferred route,
generalize the comparison beyond the supplied dimensions, rank or select an
overall winner, admit, reject, revise, route, contact, test, execute, or
disposition a candidate. Do not modify or reopen `CMD-0001`. External action is `none`.

## Evidence

<evidence path="candidates/MECHANISM-ALTERNATIVE-MAP-TEMPLATE.md">
An alternative map preserves no action and genuinely different alternatives.
It may keep a dominated alternative visible as negative evidence and must not
fabricate precision, parity, cardinality, or ranking merely to complete the
map.
</evidence>

<evidence path="GOVERNANCE.md">
Existing-owner fit defeats admission. Interest, expertise, convenience,
mailbox material, and source payloads do not create owner authority.
</evidence>

<evidence fixture="SYNTHETIC-DOMINATED-ALT-MAP-001-HEADER">
Map `SYNTH-DOMINATED-ALT-MAP-001@r1` concerns source inquiry
`SYNTH-SOURCE-DOMINATED-ALT-001@r1` at revision
`synthetic-source-revision:dominated-alt-001-r1` and problem
`SYNTH-PROBLEM-DOMINATED-ALT-001@r1`. Map owner is
`synthetic-mechanism-map-owner`; preparation authority is
`synthetic-alternative-map-preparation@r1`; candidate in scope is `none`;
external action is `none`.
</evidence>

<evidence fixture="SYNTHETIC-DOMINATED-ALT-MAP-001-PROBLEM">
One frozen synthetic correction request has no visible return route. Included
scope is only its immutable pointer, checksum, correction status, return route,
copied-content count, synchronization requirement, stops, and recovery. Real
owners, people, institutions, native content meaning, effectiveness, adoption,
and deployment are excluded.
</evidence>

<evidence fixture="SYNTHETIC-DOMINATED-ALT-MAP-001-NO-ACTION">
Alternative `SYNTH-ALT-DOMINATED-NO-ACTION-001@r1` preserves the invisible
return route. Owner route is unresolved. It adds zero copied fields and zero
synchronization obligations; correction evidence can remain orphaned. Stop on
any source, authority, standing, owner, custody, content, or risk revision.
Recovery preserves the frozen request and creates no route.
</evidence>

<evidence fixture="SYNTHETIC-DOMINATED-ALT-MAP-001-POINTER">
Alternative `SYNTH-ALT-DOMINATED-POINTER-001@r1` exposes a pointer-only return
route containing the immutable request pointer, checksum, correction owner,
stop, and acknowledgment state. It copies zero native-content fields and adds
no synchronization obligation. Owner route and acceptance are unresolved.
Failure modes are false owner authority and acknowledgment treated as
acceptance. Recovery removes the proposed route and preserves the request.
</evidence>

<evidence fixture="SYNTHETIC-DOMINATED-ALT-MAP-001-MIRROR">
Alternative `SYNTH-ALT-DOMINATED-MIRROR-001@r1` creates a mirrored return card
with the same pointer, checksum, correction owner, stop, acknowledgment state,
and intended visibility effect as
`SYNTH-ALT-DOMINATED-POINTER-001@r1`, plus three copied native-content fields
and a synchronization obligation. Under the frozen facts the copied fields add
no causal step, standing, correction capability, acceptance, or intended
effect. They add stale-copy and native-truth-absorption failure modes. The
mirror is materially distinct because it copies native truth and creates
synchronization risk, but it is dominated by the pointer route on exactly the
supplied content-copy and synchronization dimensions. Preserve it as negative
evidence. Do not infer dominance on legitimacy, adoption, effectiveness, or
real-world risk, which remain unobserved.
</evidence>

<evidence fixture="SYNTHETIC-DOMINATED-ALT-MAP-001-COMPARISON">
All evidence is synthetic. One synthetic reviewer role represents standing;
no real affected party or owner is represented. Better-existing-owner result
is `unresolved`. The supported prepared return is `defer`; it is not admission
or disposition. Preserve negative evidence at
`synthetic://map-evidence/SYNTH-DOMINATED-ALT-MAP-001@r1`.
</evidence>

<evidence fixture="SYNTHETIC-DOMINATED-ALT-MAP-001-CONTROLS">
Exact next wake is an immutable owner-authority and acceptance record,
affected-party standing, or a revision to source, pointer, checksum, copied
content, synchronization, custody, or risk. Recovery preserves the frozen
request and removes no executed state because nothing has been executed.
</evidence>

## Work now

Draft
`candidates/MECHANISM-ALTERNATIVE-MAP-DOMINATED-ALTERNATIVE-BOUNDARY-FIXTURE.md`
with YAML frontmatter (`artifact_type:
synthetic_mechanism_alternative_map_dominated_alternative_boundary_fixture`,
`status: candidate_process_fixture`, `external_action: none`) and exactly these
body sections: `Use Boundary`, `Map Header`, `Frozen Inquiry And Candidate
Boundary`, `Alternative Records`, `Material Difference And Nonduplication`,
`Existing-Owner Fit`, `Evidence, Affected Parties, And Legitimacy`,
`Comparative Boundary`, `Prepared Return`, and `Frontier Verification`.

Keep the mirror visible as a materially distinct but dominated alternative on
only the supplied dimensions. Preserve every identifier, stop, correction,
recovery, missing-evidence boundary, non-effect, and comparison limit. First line must be `---`; do not use a code fence. Return only the finished artifact.
