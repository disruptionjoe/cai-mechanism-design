# Preserve incommensurable synthetic alternatives without a fake winner

## Boundary

Write one complete synthetic Mechanism Design process fixture from only the
embedded evidence. Preserve no action and two materially different routes whose
advantages cannot be collapsed into one supported ranking. Do not invent a
shared utility scale, weights, probabilities, precision, parity, cardinality,
or an overall winner. Do not admit, reject, revise, route, contact, test,
execute, or disposition a candidate. Do not modify or reopen `CMD-0001`.
External action is `none`.

## Evidence

<evidence path="candidates/MECHANISM-ALTERNATIVE-MAP-TEMPLATE.md">
An alternative map preserves no action and genuinely different mechanisms. It
must name comparable and incommensurable dimensions and must not fabricate
precision, parity, or ranking merely to complete the map.
</evidence>

<evidence path="GOVERNANCE.md">
Existing-owner fit defeats admission. Interest, expertise, convenience,
mailbox material, and source payloads do not create owner authority.
</evidence>

<evidence fixture="SYNTHETIC-INCOMMENSURABLE-ALT-MAP-001-HEADER">
Map `SYNTH-INCOMMENSURABLE-ALT-MAP-001@r1` concerns source inquiry
`SYNTH-SOURCE-INCOMMENSURABLE-ALT-001@r1` at revision
`synthetic-source-revision:incommensurable-alt-001-r1` and problem
`SYNTH-PROBLEM-INCOMMENSURABLE-ALT-001@r1`. Map owner is
`synthetic-mechanism-map-owner`; preparation authority is
`synthetic-alternative-map-preparation@r1`; candidate in scope is `none`;
external action is `none`.
</evidence>

<evidence fixture="SYNTHETIC-INCOMMENSURABLE-ALT-MAP-001-PROBLEM">
One frozen synthetic correction record needs a visible return without copying
native content or implying acceptance. Included scope is only its immutable
pointer, checksum, correction status, acknowledgment state, review timing,
verification dependency, stops, and recovery. Real owners, people,
institutions, native content meaning, effectiveness, adoption, and deployment
are excluded.
</evidence>

<evidence fixture="SYNTHETIC-INCOMMENSURABLE-ALT-MAP-001-NO-ACTION">
Alternative `SYNTH-ALT-INCOMMENSURABLE-NO-ACTION-001@r1` preserves the
invisible return. Owner route is unresolved. It adds no copied fields,
synchronization, verification dependency, or false-acceptance surface, but the
correction can remain orphaned. Stop on any source, authority, standing,
custody, content, or risk revision. Recovery preserves the frozen record and
creates no route.
</evidence>

<evidence fixture="SYNTHETIC-INCOMMENSURABLE-ALT-MAP-001-DIRECT">
Alternative `SYNTH-ALT-INCOMMENSURABLE-DIRECT-001@r1` exposes a pointer-only
return containing the immutable pointer, checksum, correction owner, stop, and
acknowledgment state as soon as the synthetic return surface opens. It copies
zero native-content fields and adds no synchronization obligation. Its
supported advantage is earlier visibility. Owner route and acceptance are
unresolved. Failure modes are false owner authority and acknowledgment treated
as acceptance. Recovery removes the proposed route and preserves the record.
</evidence>

<evidence fixture="SYNTHETIC-INCOMMENSURABLE-ALT-MAP-001-VERIFIED">
Alternative `SYNTH-ALT-INCOMMENSURABLE-VERIFIED-001@r1` exposes the same
pointer-only fields only after an independent synthetic checksum reviewer has
matched the immutable pointer and checksum. It copies zero native-content
fields and adds no synchronization obligation. Its supported advantage is an
independent checksum match before visibility; its cost is that the return
remains invisible until that review occurs. The reviewer has no owner or
acceptance authority. Failure modes are review delay, unavailable review, false
reviewer authority, and acknowledgment treated as acceptance. Recovery removes
the proposed route and preserves the record.
</evidence>

<evidence fixture="SYNTHETIC-INCOMMENSURABLE-ALT-MAP-001-COMPARISON">
The direct and verified routes are materially distinct because one exposes the
return before independent checksum review and one conditions visibility on
that review. Both preserve the same pointer-only content, unresolved owner
route, non-acceptance boundary, and recovery. The frozen evidence supports
earlier visibility for the direct route and pre-visibility checksum review for
the verified route. It supplies no affected-party or owner weighting between
those values, no common utility scale, and no evidence that either advantage
dominates the other. Neither route is a duplicate or dominated alternative.
</evidence>

<evidence fixture="SYNTHETIC-INCOMMENSURABLE-ALT-MAP-001-CONTROLS">
All evidence is synthetic. One synthetic reviewer role represents standing;
no real affected party or owner is represented. Better-existing-owner result
is `unresolved`. Supported prepared return is `defer`; it is not admission or
disposition. Preserve the comparison at
`synthetic://map-evidence/SYNTH-INCOMMENSURABLE-ALT-MAP-001@r1`. Exact next
wake is an immutable owner-authority and acceptance record, affected-party
weighting for the two supplied values, or a revision to source, pointer,
checksum, custody, review dependency, or risk. Recovery preserves the frozen
record and removes no executed state because nothing has executed.
</evidence>

## Work now

Draft
`candidates/MECHANISM-ALTERNATIVE-MAP-INCOMMENSURABLE-DIMENSIONS-BOUNDARY-FIXTURE.md`
with YAML frontmatter (`artifact_type:
synthetic_mechanism_alternative_map_incommensurable_dimensions_boundary_fixture`,
`status: candidate_process_fixture`, `external_action: none`) and exactly these
body sections: `Use Boundary`, `Map Header`, `Frozen Inquiry And Candidate
Boundary`, `Alternative Records`, `Material Difference And Nonduplication`,
`Existing-Owner Fit`, `Evidence, Affected Parties, And Legitimacy`,
`Comparative Boundary`, `Prepared Return`, and `Frontier Verification`.

Preserve no action and both materially distinct routes. State the two supported
advantages without selecting, averaging, or ranking them. Preserve every
identifier, stop, correction, recovery, missing-evidence boundary, non-effect,
and comparison limit. First line must be `---`; close the YAML frontmatter with
a second `---`; use Markdown headings; do not use a code fence. Return only the
finished artifact.
