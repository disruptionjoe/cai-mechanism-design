# Preserve performed route opening without inventing delivery or intake

## Boundary

Write one complete synthetic Mechanism Design process fixture from only the
embedded evidence. Preserve no action and two materially distinct pointer-only
routes. Current comparison, selection, destination acceptance, and an exact
one-use route-opening grant are followed by one valid route-opening trace and
receipt. The trace proves only that the authorized synthetic actor opened the
selected pointer route after the required checks. Route opening is not contact,
sending, delivery, acknowledgment, native-content copy, intake execution,
candidate admission, disposition, or owner effect. Preserve the opened route
without inventing downstream performance. Do not invent probabilities,
expected values, parity, weights, a common utility scale, affected-party
preference, acknowledgment, or an overall outcome beyond the supplied
relation. Do not admit, reject, revise, contact, route to a real owner, send,
test, execute intake, or disposition a candidate. Do not modify or reopen
`CMD-0001` or create `CMD-0001-P8`. External action is `none`.

## Evidence

<evidence path="candidates/MECHANISM-ALTERNATIVE-MAP-TEMPLATE.md">
An alternative map may preserve comparison, selection, destination acceptance,
route-opening authority, and route-opening performance while keeping each
authority and effect separate. A valid trace proves only the exact performed
effect it records.
</evidence>

<evidence path="GOVERNANCE.md">
Existing-owner fit defeats admission. Interest, expertise, convenience,
mailbox material, source payloads, comparison, selection, acceptance,
authorization, and pointer-route opening do not create candidate admission,
intake execution, owner-effect evidence, or a stronger disposition.
</evidence>

<evidence path="candidates/MECHANISM-ALTERNATIVE-MAP-AUTHORIZED-ROUTE-OPENING-NO-PERFORMANCE-BOUNDARY-FIXTURE.md">
The prior fixture preserves an exact one-use route-opening grant in state
`authorized_not_opened`, with a non-operative `route_to_existing_owner` return.
Its next wake is one valid opening trace and receipt under the frozen grant.
</evidence>

<evidence fixture="SYNTHETIC-OPENED-ROUTE-NO-DELIVERY-001-HEADER">
Map `SYNTH-OPENED-ROUTE-NO-DELIVERY-MAP-001@r1` concerns inquiry
`SYNTH-SOURCE-OPENED-ROUTE-NO-DELIVERY-001@r1` at revision
`synthetic-source-revision:opened-route-no-delivery-001-r1` and problem
`SYNTH-PROBLEM-OPENED-ROUTE-NO-DELIVERY-001@r1`. Map owner is
`synthetic-mechanism-map-owner`; preparation authority is
`synthetic-alternative-map-preparation@r1`; candidate in scope is `none`;
external action is `none`.
</evidence>

<evidence fixture="SYNTHETIC-OPENED-ROUTE-NO-DELIVERY-001-ALTERNATIVES">
`SYNTH-ALT-OPENED-NO-ACTION-001@r1` preserves the invisible correction and
creates no route. `SYNTH-ALT-OPENED-IMMEDIATE-001@r1` exposes only the immutable
pointer, checksum, correction owner, stop, and acknowledgment state when
opened. `SYNTH-ALT-OPENED-VERIFIED-001@r1` exposes the same fields only after an
independent checksum match. Neither operative route copies native content or
adds synchronization. The verified route remains a materially distinct
dominated alternative and negative evidence on required timing.
</evidence>

<evidence fixture="SYNTHETIC-OPENED-ROUTE-NO-DELIVERY-001-COMPARISON">
Immutable record `SYNTH-CONDITION-IMMEDIATE-REQUIRED-006@r1` is signed by
`synthetic-condition-owner` under
`synthetic-condition-comparison-authority@r1`, matches the frozen source, and
makes the immediate route dominate the verified route only on required timing.
Its exact scope supplies no selection, acceptance, route-opening, contact,
admission, disposition, or execution authority.
</evidence>

<evidence fixture="SYNTHETIC-OPENED-ROUTE-NO-DELIVERY-001-SELECTION">
Immutable record `SYNTH-ROUTE-SELECTION-IMMEDIATE-004@r1` is signed by
`synthetic-route-selection-owner` under
`synthetic-route-selection-authority@r1`. Its exact scope selects
`SYNTH-ALT-OPENED-IMMEDIATE-001@r1` for one prepared pointer-only handoff and
rejects no alternative. It supplies no acceptance, route-opening, content-copy,
contact, admission, disposition, or execution authority.
</evidence>

<evidence fixture="SYNTHETIC-OPENED-ROUTE-NO-DELIVERY-001-ACCEPTANCE">
Immutable record `SYNTH-DESTINATION-ACCEPTANCE-IMMEDIATE-003@r1` is signed by
`synthetic-destination-owner` under
`synthetic-destination-pointer-intake-acceptance@r1`. Its exact scope accepts
the selected immutable pointer and checksum for one owner-controlled intake
after separately authorized route opening. It resolves better-existing-owner
fit to `synthetic-destination-owner`. It does not open the route, authorize
contact or sending, acknowledge receipt, copy native content, execute intake,
accept a candidate, choose disposition, or authorize external action.
</evidence>

<evidence fixture="SYNTHETIC-OPENED-ROUTE-NO-DELIVERY-001-AUTHORIZATION">
Immutable record `SYNTH-ROUTE-OPENING-AUTHORIZATION-002@r1` is signed by
`synthetic-route-opening-authority-owner` under
`synthetic-route-opening-decision-authority@r1`. It authorizes
`synthetic-route-opening-actor` to open
`SYNTH-ALT-OPENED-IMMEDIATE-001@r1` exactly once, exposing only the selected
immutable pointer, checksum, correction owner, stop, and acknowledgment state,
after verifying current source, pointer, checksum, custody, selection, and
acceptance matches. It authorizes no contact message, sending event, delivery,
acknowledgment, native-content copy, intake execution, candidate acceptance,
disposition, or external action.
</evidence>

<evidence fixture="SYNTHETIC-OPENED-ROUTE-NO-DELIVERY-001-TRACE">
At `2026-08-27T14:14:44Z`, `synthetic-route-opening-actor` performed the six
required current-record checks; each matched. Immutable trace
`SYNTH-ROUTE-OPENING-TRACE-001@r1` and receipt
`SYNTH-ROUTE-OPENING-RECEIPT-001@r1` record exactly one opening of
`SYNTH-ALT-OPENED-IMMEDIATE-001@r1` under
`SYNTH-ROUTE-OPENING-AUTHORIZATION-002@r1`. The receipt content identifier is
`synthetic-content:route-opening-receipt-opened-no-delivery-001-r1`; its
checksum is valid. The opening exposed only the permitted pointer fields in a
synthetic isolated state. The one-use grant is now consumed. Current route
state is `opened_not_sent`; final state differs from initial state only by the
recorded synthetic opening. No contact record, send receipt, delivery record,
acknowledgment, content-copy record, intake record, candidate record, or owner
effect exists.
</evidence>

<evidence fixture="SYNTHETIC-OPENED-ROUTE-NO-DELIVERY-001-CONTROLS">
No real person, owner, data, system, or external action is involved. Supported
prepared return remains `route_to_existing_owner`, but it remains non-operative
without separately authorized sending and a valid delivery or intake record.
Stops trigger on any source, pointer, checksum, custody, standing, comparison,
selection, acceptance, authorization, actor, trace, receipt, contact, sending,
delivery, acknowledgment, route, intake, authority, or risk revision. Failure
modes are opening treated as contact, route state treated as delivery,
acceptance treated as acknowledgment, owner fit treated as admission, consumed
authority reused, and intake invented without a receipt. Evidence pointer is
`synthetic://map-evidence/SYNTH-OPENED-ROUTE-NO-DELIVERY-001@r1`; authority
boundary is
`synthetic://execution-boundary/SYNTH-OPENED-ROUTE-NO-DELIVERY-001@r1`.
Correction owner is `synthetic-mechanism-map-owner`. Recovery closes the opened
synthetic route, preserves every record and alternative, marks the grant
consumed, restores the prepared route to acceptance-only state, and performs no
sending or intake. Exact next wake is one separately authorized send plus a
valid delivery or intake receipt under frozen current records, or any revision
to source, pointer, checksum, custody, standing, comparison, selection,
acceptance, authorization, actor, trace, receipt, contact, sending, delivery,
acknowledgment, route, intake, authority, or risk.
</evidence>

## Work now

Draft
`candidates/MECHANISM-ALTERNATIVE-MAP-OPENED-ROUTE-NO-DELIVERY-AUTHORITY-BOUNDARY-FIXTURE.md`
with YAML frontmatter (`artifact_type:
synthetic_mechanism_alternative_map_opened_route_no_delivery_authority_boundary_fixture`,
`status: candidate_process_fixture`, `external_action: none`) and exactly these
body sections: `Use Boundary`, `Map Header`, `Frozen Inquiry And Candidate
Boundary`, `Alternative Records`, `Material Difference And Nonduplication`,
`Existing-Owner Fit`, `Evidence, Affected Parties, And Legitimacy`,
`Comparison, Selection, Acceptance, And Authorization Boundary`,
`Route-Opening Trace And Downstream Nonperformance`, `Prepared Return And
Recovery`, and `Frontier Verification`.

Keep comparison, selection, destination acceptance, route-opening authority,
route-opening performance, contact, sending, delivery, acknowledgment, and
intake execution distinct. Preserve every identifier, signer, exact authority
scope, trace and receipt identifier, receipt content identifier, checksum
validity, consumed grant, absent downstream records, stop, failure mode,
correction, recovery, evidence pointer, non-effect, and wake. In `Frontier
Verification`, explicitly check current record matches, exact one-use grant,
valid trace and receipt, permitted exposed fields, consumed authority,
`opened_not_sent`, no contact/send/delivery/acknowledgment/intake evidence,
resolved owner fit without admission, retained dominated alternative,
non-operative `route_to_existing_owner`, and unchanged `CMD-0001` and
external-action state. First line must be `---`; close frontmatter with a later
line containing only `---`; immediately after frontmatter use the title
`# Synthetic Mechanism Alternative-Map Opened-Route No-Delivery-Authority Boundary Fixture`;
use `##` for every named body section and deeper headings only within a named
section; do not use a code fence.

Return only the finished artifact.
