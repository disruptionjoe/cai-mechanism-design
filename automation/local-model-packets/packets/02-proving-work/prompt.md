# Inventory explicit affected-party standing evidence in SFQ-0001

Family lineage: `MD-LIN-SFQ-0001-AFFECTED-PARTY-STANDING-INVENTORY-01`,
opportunity 1. Begin the finished artifact immediately. Do not browse,
prescribe, or add facts.

Frozen source-owner evidence:

- `SF-0001` contains no explicit affected-party phrase in the preserved class
  or domain boundary supplied here.
- `SF-0002` contains no explicit affected-party phrase in the preserved class
  or domain boundary supplied here.
- `SF-0003` contains `resident-facing notice` in its preserved intervention or
  correction class.
- `SF-0004` contains `worker safety` in its domain boundary.
- `SF-0005` contains `resident-warning routes` in its preserved intervention
  or correction class.

These phrases do not establish affected-party standing, participation,
representation, appeal, legitimacy, effectiveness, or remedy ownership. The
only supported common residue is owner-visible changed evidence,
affected-party standing, and a correction or stop route. Domain owners retain
remedy meaning, authority, and evaluation. `CMD-0001` remains deferred.

Return only
`candidates/SFQ-0001-AFFECTED-PARTY-STANDING-INVENTORY-DRAFT.md`. The first
line must be `---`; do not use a code fence. Use valid YAML frontmatter with
`artifact_type: affected_party_standing_inventory_draft`,
`status: unverified_local_candidate`, `source_inquiry: SFQ-0001`, and
`external_action: none`. Then one title and exactly three `##` sections: Use
Boundary; Standing Inventory; Frontier Verification And Stops.

Under `## Standing Inventory`, return one Markdown table with exactly five
rows and this literal header:

`Record | Explicit affected-party phrase | Source location type | Standing
claim | Participation or appeal claim | Legitimacy claim | Unknowns and owner
boundary`

Use `unknown` for `SF-0001` and `SF-0002` affected-party phrases. Copy the
other three phrases exactly. Source location type is literal `preserved class`,
`domain boundary`, or `unknown` as supplied above. Standing, participation or
appeal, and legitimacy are literal `unknown` for all five rows. The final cell
states that domain owners retain remedy authority and no effect is authorized.
Maximum 500 words. Do not admit a candidate, reopen `CMD-0001`, authorize
proving, contact anyone, or create an external effect. Return no code fence.
