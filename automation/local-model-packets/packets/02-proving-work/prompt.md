# Inventory the SFQ-0001 visibility-to-correction link boundary

Family lineage: `MD-LIN-SFQ-0001-VISIBILITY-CORRECTION-LINK-INVENTORY-01`,
opportunity 1. Begin the finished artifact immediately. Do not browse,
prescribe, or add facts.

Frozen source-owner evidence:

- `SF-0001`: visibility `alert clarity; certification visibility`; correction
  or stop `safety-assessment revalidation`.
- `SF-0002`: visibility `imagery or inspection`; correction or stop `hazard
  control; accountable escalation`.
- `SF-0003`: visibility `resident-facing notice`; correction or stop
  `corrosion control; exposure mitigation`.
- `SF-0004`: visibility `unknown`; correction or stop `stop, retest, escalate,
  or revise a barrier assumption when test evidence remains contradictory`.
- `SF-0005`: visibility `resident-warning routes`; correction or stop
  `building and product assurance`.

The frozen evidence does not explicitly establish that any visibility role
causes, triggers, authorizes, or completes its paired correction-or-stop role.
Affected-party standing and effectiveness are unknown for every row. Domain
owners retain remedy meaning, authority, and evaluation. `CMD-0001` remains
deferred.

Return only
`candidates/SFQ-0001-VISIBILITY-CORRECTION-LINK-INVENTORY-DRAFT.md`. The first
line must be `---`; do not use a code fence. Use valid YAML frontmatter with
`artifact_type: visibility_correction_link_inventory_draft`,
`status: unverified_local_candidate`, `source_inquiry: SFQ-0001`, and
`external_action: none`. Then one title and exactly three `##` sections: Use
Boundary; Link Inventory; Frontier Verification And Stops.

Under `## Link Inventory`, return one Markdown table with exactly five rows
and this literal header:

`Record | Visibility role | Correction or stop role | Explicit linkage claim
| Affected-party standing | Effectiveness evidence | Unknowns and owner
boundary`

Copy every frozen role exactly. Explicit linkage claim, affected-party
standing, and effectiveness evidence are literal `unknown` for every row. The
final cell states that domain owners retain remedy authority and no effect is
authorized. Maximum 550 words. Do not admit a candidate, reopen `CMD-0001`,
authorize proving, contact anyone, or create an external effect. Return no code
fence.
