# Route SFQ-0001 evidence needs without acquiring evidence

Family lineage: `MD-LIN-SFQ-0001-EVIDENCE-ROUTE-MATRIX-01`, opportunity 1.
Begin the finished artifact immediately. Do not browse, name a specific source,
answer a question, rank records, infer authority or standing, prescribe, or
contact anyone.

Frozen source-owner evidence: the current source-question boundary for each of
`SF-0001` through `SF-0005` is:

- visibility-to-authority and authority-to-correction linkage are unknown;
- affected-party standing, participation, appeal, and correction access are
  unknown;
- invocation and response ownership, implementation cost, enforceability, and
  prevention-versus-repair posture are unknown; and
- effectiveness and new capture surfaces are unknown.

Domain owners retain remedy meaning, authority, and evaluation.
`CMD-0001` remains deferred. This packet may form evidence requirements only.
It cannot acquire evidence, authorize research or proving, or create an
external effect.

Return only `candidates/SFQ-0001-EVIDENCE-ROUTE-MATRIX-DRAFT.md`. The first line
must be `---`; do not use a code fence. Use valid YAML frontmatter with
`artifact_type: evidence_route_matrix_draft`,
`status: unverified_local_candidate`, `source_inquiry: SFQ-0001`, and
`external_action: none`. Then one title and exactly three `##` sections: Use
Boundary; Evidence Route Matrix; Frontier Verification And Stops.

Under `## Evidence Route Matrix`, return one Markdown table with exactly five
rows and this literal header:

`Record | Linkage evidence needed | Standing and access evidence needed | Implementation evidence needed | Outcome and capture evidence needed | Permitted public source categories | Source status | Owner boundary`

Each evidence-needed cell must state what an eventual public record would need
to explicitly establish without claiming that such a record exists. The
permitted categories are limited to `official investigation or audit`,
`governing rule or formal authority record`, `public implementation or outcome
record`, and `affected-party public record`; do not name sources. `Source
status` is literal `not_acquired` for every row. Every final cell says `Domain
owners retain remedy authority and no effect is authorized.` Maximum 800
words. Do not admit a candidate, reopen `CMD-0001`, authorize research or
proving, contact anyone, or create an external effect. Return no other text.
Return no code fence.
