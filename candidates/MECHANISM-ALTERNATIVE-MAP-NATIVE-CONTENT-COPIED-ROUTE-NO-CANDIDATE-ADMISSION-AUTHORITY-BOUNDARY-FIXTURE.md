---
artifact_type: synthetic_mechanism_alternative_map_native_content_copied_route_no_candidate_admission_authority_boundary_fixture
status: candidate_process_fixture
external_action: none
---

# Synthetic Mechanism Alternative-Map Native-Content-Copied-Route No-Candidate-Admission-Authority Boundary Fixture

## Use Boundary

This synthetic fixture applies `MECHANISM-ALTERNATIVE-MAP-TEMPLATE.md` to the
frozen lineage in
`MECHANISM-ALTERNATIVE-MAP-INTAKEN-ROUTE-NO-CANDIDATE-ADMISSION-AUTHORITY-BOUNDARY-FIXTURE.md`.
It adds one separately authorized native-content copy into an owner-controlled
staging surface. It does not admit a candidate, establish candidate status,
accept a result, disposition work, graduate, deploy, create a target-native
accepted effect, modify `CMD-0001`, create `CMD-0001-P8`, or authorize external
action.

## Map Header

| Field | Value |
| --- | --- |
| `map_id` | `SYNTH-CONTENT-COPIED-ROUTE-NO-CANDIDATE-001@r1` |
| `source_inquiry` | `SYNTH-SOURCE-OPENED-ROUTE-NO-DELIVERY-001@r1` |
| `source_revision` | `synthetic-source-revision:opened-route-no-delivery-001-r1` |
| `candidate_or_problem_ref` | `SYNTH-PROBLEM-OPENED-ROUTE-NO-DELIVERY-001@r1`; candidate scope `none` |
| `map_owner` | `synthetic-mechanism-map-owner` |
| `authorization_ref` | `synthetic://execution-boundary/SYNTH-CONTENT-COPIED-ROUTE-NO-CANDIDATE-001@r1` |
| `external_action` | `none` |

## Frozen Inquiry And Candidate Boundary

The frozen problem, source, and revision remain unchanged. The map may preserve
the no-action, immediate, and verified pointer routes and the exact
comparison-through-intake lineage, then record only the named synthetic copy.
Existing-owner fit resolves to `synthetic-destination-owner` and defeats
admission here. No real person, owner, data, or system is involved, and no
source claim, owner truth, candidate, or downstream decision may be inferred.

## Alternative Records

- `SYNTH-ALT-OPENED-NO-ACTION-001@r1` preserves no action.
- `SYNTH-ALT-OPENED-IMMEDIATE-001@r1` preserves the selected immediate
  pointer-only route.
- `SYNTH-ALT-OPENED-VERIFIED-001@r1` remains materially distinct dominated
  evidence on required timing; it is not erased by selection or performance
  of the immediate route.

The alternatives remain synthetic, reversible records. None is a candidate,
disposition, destination result, graduation, deployment, or external action.

## Material Difference And Nonduplication

The no-action route performs nothing. The immediate route preserves the
authorized pointer lineage and the one exact native-content copy. The verified
route preserves a distinct verification step despite its timing disadvantage.
The copy is not a duplicate of intake: intake brought the pointer package into
the boundary, whereas the copy placed only the named immutable payload on the
destination-controlled staging surface under separate authority. No variants
are merged and no overall ranking beyond the frozen timing comparison is
created.

## Existing-Owner Fit

Existing-owner fit is `synthetic-destination-owner`, based on the destination's
native staging and intake authority. That fit defeats candidate admission in
Mechanism Design. Interest, execution history, or a staged copy does not expand
the destination's authority, establish accepted owner truth, or give this map
authority to admit, accept, disposition, graduate, or deploy anything.

## Evidence, Affected Parties, And Legitimacy

Evidence pointer is
`synthetic://map-evidence/SYNTH-CONTENT-COPIED-ROUTE-NO-CANDIDATE-001@r1`.
All evidence, actors, standing, and risks are synthetic. No affected person or
real owner is represented or can be spoken for. The principal legitimacy risk
is authority laundering: treating a valid staged copy as candidate admission,
accepted truth, or downstream authority. Correction belongs to
`synthetic-mechanism-map-owner`.

## Comparison Through Intake Boundary

Comparison `SYNTH-CONDITION-IMMEDIATE-REQUIRED-006@r1` is signed by
`synthetic-condition-owner` under
`synthetic-condition-comparison-authority@r1`. Selection
`SYNTH-ROUTE-SELECTION-IMMEDIATE-004@r1` is signed by
`synthetic-route-selection-owner` under
`synthetic-route-selection-authority@r1`. Acceptance
`SYNTH-DESTINATION-ACCEPTANCE-IMMEDIATE-003@r1` is signed by
`synthetic-destination-owner` under
`synthetic-destination-pointer-intake-acceptance@r1` and stays limited to the
named owner-controlled boundary.

Opening authorization `SYNTH-ROUTE-OPENING-AUTHORIZATION-002@r1`, trace
`SYNTH-ROUTE-OPENING-TRACE-001@r1`, receipt
`SYNTH-ROUTE-OPENING-RECEIPT-001@r1`, and content ID
`synthetic-content:route-opening-receipt-opened-no-delivery-001-r1` remain
valid. Sending authorization `SYNTH-ROUTE-SENDING-AUTHORIZATION-001@r1`, trace
`SYNTH-ROUTE-SENDING-TRACE-001@r1`, receipt
`SYNTH-ROUTE-SENDING-RECEIPT-001@r1`, and content ID
`synthetic-content:route-sending-receipt-sent-no-delivery-001-r1` remain valid.
Delivery authorization `SYNTH-ROUTE-DELIVERY-AUTHORIZATION-001@r1`, trace
`SYNTH-ROUTE-DELIVERY-TRACE-001@r1`, receipt
`SYNTH-ROUTE-DELIVERY-RECEIPT-001@r1`, and content ID
`synthetic-content:route-delivery-receipt-delivered-no-acknowledgment-001-r1`
remain valid. Acknowledgment authorization
`SYNTH-ROUTE-ACKNOWLEDGMENT-AUTHORIZATION-001@r1`, trace
`SYNTH-ROUTE-ACKNOWLEDGMENT-TRACE-001@r1`, receipt
`SYNTH-ROUTE-ACKNOWLEDGMENT-RECEIPT-001@r1`, and content ID
`synthetic-content:route-acknowledgment-receipt-acknowledged-no-intake-001-r1`
remain valid.

Intake authorization `SYNTH-ROUTE-INTAKE-AUTHORIZATION-001@r1` is signed by
`synthetic-destination-owner` under
`synthetic-destination-pointer-intake-execution-authority@r1`. Trace
`SYNTH-ROUTE-INTAKE-TRACE-001@r1` and receipt
`SYNTH-ROUTE-INTAKE-RECEIPT-001@r1` record exactly one valid pointer intake at
`2026-08-27T18:20:08Z`. Receipt content ID is
`synthetic-content:route-intake-receipt-intaken-no-candidate-001-r1`, its
checksum is valid, all five prior one-use grants are consumed, and the prior
route state is `intaken_not_admitted`.

## Native-Content-Copy Trace And Downstream Nonperformance

At `2026-08-27T20:15:24Z`, authorization
`SYNTH-ROUTE-NATIVE-CONTENT-COPY-AUTHORIZATION-001@r1` is signed by
`synthetic-destination-owner` under
`synthetic-destination-native-content-copy-execution-authority@r1`. It
authorizes `synthetic-destination-content-copy-actor` exactly once to copy only
payload `SYNTH-NATIVE-CONTENT-PAYLOAD-001@r1`, content ID
`synthetic-content:native-content-payload-001-r1`, into
`synthetic-destination-staging-surface`. Before copying, the actor must confirm
unchanged source, pointer, payload, checksums, custody, comparison, selection,
acceptance, opening, sending, delivery, acknowledgment, intake, and unconsumed
copy authority.

At `2026-08-27T20:20:24Z`, every required check matched. Trace
`SYNTH-ROUTE-NATIVE-CONTENT-COPY-TRACE-001@r1` and receipt
`SYNTH-ROUTE-NATIVE-CONTENT-COPY-RECEIPT-001@r1` record exactly one copy of the
named payload. Receipt content ID is
`synthetic-content:route-native-content-copy-receipt-copied-no-candidate-001-r1`,
its checksum is valid, and the one-use copy grant is consumed. Route state is
`native_content_copied_not_admitted`; prepared return is
`native_content_copy_only`. No candidate, candidate-status, disposition,
result-acceptance, graduation, deployment, or target-native accepted-effect
record exists.

## Prepared Return And Recovery

Preserve every predecessor record and alternative. Stop on any named source,
payload, record, receipt, actor, authority, standing, route, intake, copy,
candidate, or risk revision. Failure modes are treating the staged copy as
candidate admission or owner truth, silently changing copied content, reusing
intake authority as copy authority, reusing a consumed grant, or inventing a
downstream decision. If the copy authority or checksum fails, retract only the
copy, restore `intaken_not_admitted`, and create no candidate. Exact next wake
is one separately authorized candidate-admission decision under frozen current
records, or a revision to any named fact.

## Frontier Verification

Verify the frozen source, map, alternatives, signer, authority scopes, actor,
payload, traces, receipts, content IDs, checksums, six consumed grants, and
absent downstream records. Re-run existing-owner fit, nonduplication,
affected-party, legitimacy, stop, correction, and recovery checks. Confirm the
copy changes only route state to `native_content_copied_not_admitted`, preserves
no action and the dominated route, and authorizes no candidate, disposition,
accepted result, graduation, deployment, target-native effect, or external
action.
