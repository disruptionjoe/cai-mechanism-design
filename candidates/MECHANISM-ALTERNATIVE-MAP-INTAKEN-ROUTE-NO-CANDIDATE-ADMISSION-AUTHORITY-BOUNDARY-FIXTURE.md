---
artifact_type: synthetic_mechanism_alternative_map_intaken_route_no_candidate_admission_authority_boundary_fixture
status: candidate_process_fixture
external_action: none
---

# Synthetic Mechanism Alternative-Map Intaken-Route No-Candidate-Admission-Authority Boundary Fixture

## Use Boundary

This fixture preserves no action and two materially distinct pointer-only
routes while adding exactly one separately authorized synthetic pointer-package
intake. Intake proves only that the destination-controlled synthetic boundary
registered the immutable pointer package in its owner-controlled intake
surface. It is not native-content copy, candidate admission, candidate status,
disposition, result acceptance, graduation, deployment, or a target-native
owner-truth effect. Existing-owner fit defeats admission. No real person,
owner, data, system, or external action is involved. `CMD-0001` and
`CMD-0001-P8` remain outside this fixture.

## Map Header

- Governing template: `candidates/MECHANISM-ALTERNATIVE-MAP-TEMPLATE.md`
- Predecessor fixture:
  `candidates/MECHANISM-ALTERNATIVE-MAP-ACKNOWLEDGED-ROUTE-NO-INTAKE-AUTHORITY-BOUNDARY-FIXTURE.md`
- Map: `SYNTH-INTAKEN-ROUTE-NO-CANDIDATE-MAP-001@r1`
- Inquiry: `SYNTH-SOURCE-OPENED-ROUTE-NO-DELIVERY-001@r1`
- Source revision:
  `synthetic-source-revision:opened-route-no-delivery-001-r1`
- Problem: `SYNTH-PROBLEM-OPENED-ROUTE-NO-DELIVERY-001@r1`
- Candidate scope: `none`
- Map owner and correction owner: `synthetic-mechanism-map-owner`
- Evidence pointer:
  `synthetic://map-evidence/SYNTH-INTAKEN-ROUTE-NO-CANDIDATE-001@r1`
- Authority boundary:
  `synthetic://execution-boundary/SYNTH-INTAKEN-ROUTE-NO-CANDIDATE-001@r1`

## Frozen Inquiry And Candidate Boundary

The named inquiry, source revision, problem, candidate scope, records, actors,
authorities, and risk boundary are frozen. Existing-owner fit resolves to
`synthetic-destination-owner` and defeats admission here. The fixture may
preserve the supplied pointer-route history and exact intake only. It may not
copy native content, admit a candidate, change candidate status, accept a
result, disposition work, graduate work, deploy anything, or claim a target-
native owner-truth effect.

## Alternative Records

- `SYNTH-ALT-OPENED-NO-ACTION-001@r1` preserves the invisible correction and
  creates no route.
- `SYNTH-ALT-OPENED-IMMEDIATE-001@r1` exposes only the immutable pointer,
  checksum, correction owner, stop, and current route state.
- `SYNTH-ALT-OPENED-VERIFIED-001@r1` exposes the same fields only after an
  independent checksum match.

The verified route remains materially distinct dominated evidence on required
timing. Neither operative route copies native content.

## Material Difference And Nonduplication

The immediate route has valid comparison, selection, destination acceptance,
opening, sending, delivery, acknowledgment, and now intake records. The
verified route remains materially distinct negative evidence on required
timing. The only change from the predecessor is one authorized registration of
the already-acknowledged immutable pointer package. No native-content-copy,
candidate, disposition, result-acceptance, graduation, deployment, or target-
native owner-truth-effect record exists.

## Existing-Owner Fit

Existing-owner fit resolves to `synthetic-destination-owner` and defeats
admission here. Destination acceptance
`SYNTH-DESTINATION-ACCEPTANCE-IMMEDIATE-003@r1` is signed by
`synthetic-destination-owner` under
`synthetic-destination-pointer-intake-acceptance@r1`. It accepts only the
selected immutable pointer and checksum for one owner-controlled pointer
intake after separately authorized effects. It does not supply comparison,
selection, opening, sending, delivery, acknowledgment, intake execution,
native-content-copy, result-acceptance, or candidate-disposition authority.

## Evidence, Affected Parties, And Legitimacy

Only synthetic actors and records are involved. Comparison
`SYNTH-CONDITION-IMMEDIATE-REQUIRED-006@r1` is signed by
`synthetic-condition-owner` under
`synthetic-condition-comparison-authority@r1`. Selection
`SYNTH-ROUTE-SELECTION-IMMEDIATE-004@r1` is signed by
`synthetic-route-selection-owner` under
`synthetic-route-selection-authority@r1`. Each downstream effect has a
separate exact one-use authority, actor, trace, and receipt.

Stop on any named source, record, receipt, actor, authority, standing, route,
intake, candidate, or risk revision. Failure modes include intake treated as
candidate admission, pointer registration treated as native-content copy,
intake receipt treated as result acceptance or owner truth, consumed authority
reused, or native content invented.

## Comparison, Selection, Acceptance, Opening, Sending, Delivery, Acknowledgment, And Intake Boundary

Opening authorization `SYNTH-ROUTE-OPENING-AUTHORIZATION-002@r1`, trace
`SYNTH-ROUTE-OPENING-TRACE-001@r1`, and receipt
`SYNTH-ROUTE-OPENING-RECEIPT-001@r1` remain valid. Opening receipt content ID
is `synthetic-content:route-opening-receipt-opened-no-delivery-001-r1`.

Sending authorization `SYNTH-ROUTE-SENDING-AUTHORIZATION-001@r1` is signed by
`synthetic-route-sending-authority-owner` under
`synthetic-route-sending-decision-authority@r1`. Trace
`SYNTH-ROUTE-SENDING-TRACE-001@r1` and receipt
`SYNTH-ROUTE-SENDING-RECEIPT-001@r1` remain valid. Send receipt content ID is
`synthetic-content:route-sending-receipt-sent-no-delivery-001-r1`.

Delivery authorization `SYNTH-ROUTE-DELIVERY-AUTHORIZATION-001@r1` is signed
by `synthetic-route-delivery-authority-owner` under
`synthetic-route-delivery-decision-authority@r1`. Trace
`SYNTH-ROUTE-DELIVERY-TRACE-001@r1` and receipt
`SYNTH-ROUTE-DELIVERY-RECEIPT-001@r1` remain valid. Delivery receipt content ID
is
`synthetic-content:route-delivery-receipt-delivered-no-acknowledgment-001-r1`.

Acknowledgment authorization
`SYNTH-ROUTE-ACKNOWLEDGMENT-AUTHORIZATION-001@r1` is signed by
`synthetic-route-acknowledgment-authority-owner` under
`synthetic-route-acknowledgment-decision-authority@r1`. Trace
`SYNTH-ROUTE-ACKNOWLEDGMENT-TRACE-001@r1` and receipt
`SYNTH-ROUTE-ACKNOWLEDGMENT-RECEIPT-001@r1` remain valid. Acknowledgment
receipt content ID is
`synthetic-content:route-acknowledgment-receipt-acknowledged-no-intake-001-r1`.
The opening, sending, delivery, and acknowledgment grants are consumed. Route
state before intake is `acknowledged_not_intaken`.

At `2026-08-27T18:15:08Z`, intake authorization
`SYNTH-ROUTE-INTAKE-AUTHORIZATION-001@r1` is signed by
`synthetic-destination-owner` under
`synthetic-destination-pointer-intake-execution-authority@r1`. It authorizes
`synthetic-destination-intake-actor` exactly once to register only the already-
acknowledged immutable pointer package after checking unchanged source,
pointer, checksum, custody, comparison, selection, acceptance, opening,
sending, delivery, acknowledgment, and unconsumed intake authority. It
authorizes no native-content copy, candidate admission, candidate status,
disposition, result acceptance, graduation, deployment, target-native owner-
truth effect, or external action.

## Intake Trace And Downstream Nonperformance

At `2026-08-27T18:20:08Z`, `synthetic-destination-intake-actor` performed every
required current-record check and each matched. Trace
`SYNTH-ROUTE-INTAKE-TRACE-001@r1` and receipt
`SYNTH-ROUTE-INTAKE-RECEIPT-001@r1` record exactly one synthetic pointer-
package intake. Receipt content ID is
`synthetic-content:route-intake-receipt-intaken-no-candidate-001-r1`; its
checksum is valid. The one-use intake grant is consumed. Route state is
`intaken_not_admitted`; prepared return is `intaken_pointer_package_only`.

No native-content-copy, candidate, disposition, result-acceptance, graduation,
deployment, or target-native owner-truth-effect record exists. Intake does not
alter the scope or authority of any predecessor record.

## Prepared Return And Recovery

All five one-use grants are consumed. Recovery preserves every predecessor
record and alternative, retracts intake only if its own authority or checksum
fails, restores `acknowledged_not_intaken`, and creates no candidate. The
correction owner is `synthetic-mechanism-map-owner`.

Exact next wake is one separately authorized native-content-copy record or candidate-admission decision under frozen current records, or a revision to any named fact.

## Frontier Verification

Verify every frozen identifier, signer, authority scope, actor, trace, receipt,
content ID, checksum, and consumed grant. Verify the exact five-effect lineage,
two materially distinct routes plus no action, resolved existing-owner fit
without admission, all absent downstream records, stops, failure modes,
correction, recovery, non-effect, and exact wake. Verify unchanged `CMD-0001`,
no P8, and external action `none`.
