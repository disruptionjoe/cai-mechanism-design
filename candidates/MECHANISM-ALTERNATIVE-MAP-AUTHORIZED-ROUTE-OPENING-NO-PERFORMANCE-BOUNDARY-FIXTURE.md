---
artifact_type: synthetic_mechanism_alternative_map_authorized_route_opening_no_performance_boundary_fixture
status: candidate_process_fixture
external_action: none
---
# Synthetic Mechanism Alternative-Map Authorized-Route-Opening No-Performance Boundary Fixture

## Use Boundary

This fixture preserves a supported comparison, authorized selection,
destination-owner acceptance, and one bounded route-opening grant while
keeping authorization, performance, contact, sending, intake execution,
candidate admission, and disposition separate. An unperformed grant is not an
effect. Existing-owner fit defeats admission here.

## Map Header

Map `SYNTH-AUTHORIZED-ROUTE-NOT-OPENED-MAP-001@r1` concerns inquiry
`SYNTH-SOURCE-AUTHORIZED-ROUTE-NOT-OPENED-001@r1` at revision
`synthetic-source-revision:authorized-route-not-opened-001-r1` and problem
`SYNTH-PROBLEM-AUTHORIZED-ROUTE-NOT-OPENED-001@r1`. The map owner is
`synthetic-mechanism-map-owner`; preparation authority is
`synthetic-alternative-map-preparation@r1`; candidate in scope is `none`; and
external action is `none`.

## Frozen Inquiry And Candidate Boundary

The source, pointer, checksum custody, standing, comparison, selection,
acceptance, authorization, actor, route, authority, and risk records are frozen
at r1. No candidate is admitted, rejected, revised, routed, tested, executed,
or dispositioned. `CMD-0001` remains unchanged and no `CMD-0001-P8` exists.

## Alternative Records

- `SYNTH-ALT-AUTHORIZED-NO-ACTION-001@r1` preserves the invisible correction
  and creates no route.
- `SYNTH-ALT-AUTHORIZED-IMMEDIATE-001@r1` would expose only the immutable
  pointer, checksum, correction owner, stop, and acknowledgment state when
  actually opened.
- `SYNTH-ALT-AUTHORIZED-VERIFIED-001@r1` would expose the same fields only after
  an independent checksum match.

Neither operative route copies native content or adds synchronization. The
verified route remains a materially distinct dominated alternative and
negative evidence on required timing.

## Material Difference And Nonduplication

No action remains materially distinct from either pointer route. Immediate and
verified exposure share pointer-only custody but differ on the independent
pre-opening checksum step and therefore on supported timing. The current map
records only that supplied relation; it does not invent probabilities,
expected values, parity, weights, affected-party preferences, a common utility
scale, or an overall outcome.

## Existing-Owner Fit

Immutable record `SYNTH-DESTINATION-ACCEPTANCE-IMMEDIATE-002@r1` is signed by
`synthetic-destination-owner` under
`synthetic-destination-pointer-intake-acceptance@r1`. Its exact scope accepts
the selected immutable pointer and checksum for one owner-controlled intake
after separately authorized route opening and resolves better-existing-owner
fit to `synthetic-destination-owner`. It does not open the route, authorize
contact or sending, acknowledge receipt, copy native content, execute intake,
accept a candidate, choose a disposition, or authorize external action.

## Evidence, Affected Parties, And Legitimacy

The evidence is fully synthetic. One synthetic reviewer role represents
standing; no real person, owner, data, system, or external action is involved.
The evidence pointer is
`synthetic://map-evidence/SYNTH-AUTHORIZED-ROUTE-NOT-OPENED-001@r1` and the
authority boundary is
`synthetic://execution-boundary/SYNTH-AUTHORIZED-ROUTE-NOT-OPENED-001@r1`.
Comparison, selection, acceptance, and route-opening authority each operate
only over the exact named decision or effect.

## Comparison, Selection, And Acceptance Boundary

Immutable record `SYNTH-CONDITION-IMMEDIATE-REQUIRED-005@r1` is signed by
`synthetic-condition-owner` under
`synthetic-condition-comparison-authority@r1`, matches the frozen source, and
makes the immediate route dominate the verified route only on required timing.
It supplies no selection, acceptance, route-opening, contact, admission,
disposition, or execution authority.

Immutable record `SYNTH-ROUTE-SELECTION-IMMEDIATE-003@r1` is signed by
`synthetic-route-selection-owner` under
`synthetic-route-selection-authority@r1`. Its exact scope selects
`SYNTH-ALT-AUTHORIZED-IMMEDIATE-001@r1` for one prepared pointer-only handoff,
rejects no alternative, and supplies no acceptance, route-opening,
content-copy, contact, admission, disposition, or execution authority.

Destination acceptance remains the separate, acceptance-only record described
above. Comparison is not selection; selection is not acceptance; acceptance
is not route opening, contact, sending, or intake execution.

## Route-Opening Grant And Nonperformance

Later immutable record `SYNTH-ROUTE-OPENING-AUTHORIZATION-001@r1` is signed by
`synthetic-route-opening-authority-owner` under
`synthetic-route-opening-decision-authority@r1`. Its exact scope authorizes
`synthetic-route-opening-actor` to open
`SYNTH-ALT-AUTHORIZED-IMMEDIATE-001@r1` once, exposing only the selected
immutable pointer, checksum, correction owner, stop, and acknowledgment state,
after verifying current source, pointer, checksum, and custody matches. It
authorizes no contact message, sending event, native-content copy, intake
execution, candidate acceptance, disposition, or external action.

No route-opening trace, execution receipt, contact record, send receipt,
acknowledgment, content-copy record, or intake record exists. The authorized
actor performed nothing. Current route state is `authorized_not_opened`; final
state equals initial state. The grant proves authorization only and cannot
prove performance.

## Prepared Return And Recovery

Supported prepared return remains `route_to_existing_owner`, but it is
non-operative and creates no owner effect without a valid opening trace and
receipt. Stops trigger on any source, pointer, checksum, custody, standing,
comparison, selection, acceptance, authorization, actor, contact, route,
execution, authority, or risk revision. Failure modes are a grant treated as
performance, authorized route state treated as opened route state, acceptance
treated as contact, owner fit treated as admission, and acknowledgment
invented without a receipt.

Correction owner is `synthetic-mechanism-map-owner`. Recovery revokes the
unused grant, preserves every record and alternative, restores the prepared
route to acceptance-only state, and performs nothing. Exact next wake is one
valid route-opening trace and receipt under the frozen grant, or a revision to
source, pointer, checksum, custody, standing, comparison, selection,
acceptance, authorization, actor, contact, route, execution, authority, or
risk.

## Frontier Verification

- Current source, pointer, checksum custody, comparison, selection, acceptance,
  and authorization records match.
- Comparison-only, selection-only, acceptance-only, and exact route-opening
  scopes remain separately attributable to their named signers and authority
  records.
- Missing performance evidence is proved by the absent route-opening trace and
  receipt, not inferred from the grant.
- Owner fit is resolved without candidate admission; the verified route remains
  visible as a materially distinct dominated alternative.
- State remains `authorized_not_opened`; `route_to_existing_owner` remains
  non-operative; `CMD-0001`, candidate disposition, and external-action state
  remain unchanged.
