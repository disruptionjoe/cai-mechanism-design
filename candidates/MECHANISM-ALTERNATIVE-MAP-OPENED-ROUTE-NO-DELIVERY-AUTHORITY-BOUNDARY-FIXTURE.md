---
artifact_type: synthetic_mechanism_alternative_map_opened_route_no_delivery_authority_boundary_fixture
status: candidate_process_fixture
external_action: none
---
# Synthetic Mechanism Alternative-Map Opened-Route No-Delivery-Authority Boundary Fixture

## Use Boundary

An alternative map may preserve comparison, selection, destination acceptance,
route-opening authority, and route-opening performance while keeping each
authority and effect separate. A valid trace proves only the exact performed
effect it records. Existing-owner fit defeats admission. Interest, expertise,
convenience, mailbox material, source payloads, comparison, selection,
acceptance, authorization, and pointer-route opening do not create candidate
admission, intake execution, owner-effect evidence, or a stronger disposition.
The prior fixture preserves an exact one-use route-opening grant in state
`authorized_not_opened`, with a non-operative `route_to_existing_owner` return.
Its next wake is one valid opening trace and receipt under the frozen grant.

## Map Header

Map `SYNTH-OPENED-ROUTE-NO-DELIVERY-MAP-001@r1` concerns inquiry
`SYNTH-SOURCE-OPENED-ROUTE-NO-DELIVERY-001@r1` at revision
`synthetic-source-revision:opened-route-no-delivery-001-r1` and problem
`SYNTH-PROBLEM-OPENED-ROUTE-NO-DELIVERY-001@r1`. Map owner is
`synthetic-mechanism-map-owner`; preparation authority is
`synthetic-alternative-map-preparation@r1`; candidate in scope is `none`;
external action is `none`.

## Frozen Inquiry And Candidate Boundary

Frozen inquiry `SYNTH-SOURCE-OPENED-ROUTE-NO-DELIVERY-001@r1` maintains the
current state for `SYNTH-OPENED-ROUTE-NO-DELIVERY-MAP-001@r1`. Candidate scope
remains `none`. The inquiry revision
`synthetic-source-revision:opened-route-no-delivery-001-r1` is preserved
without modification. No candidate admission or intake execution occurs.

## Alternative Records

`SYNTH-ALT-OPENED-NO-ACTION-001@r1` preserves the invisible correction and
creates no route. `SYNTH-ALT-OPENED-IMMEDIATE-001@r1` exposes only the immutable
pointer, checksum, correction owner, stop, and acknowledgment state when
opened. `SYNTH-ALT-OPENED-VERIFIED-001@r1` exposes the same fields only after
an independent checksum match. Neither operative route copies native content
or adds synchronization. The verified route remains a materially distinct
dominated alternative and negative evidence on required timing.

## Material Difference And Nonduplication

The opened route `SYNTH-ALT-OPENED-IMMEDIATE-001@r1` is distinct from
`SYNTH-ALT-OPENED-VERIFIED-001@r1`. No duplicate content is created. Native
content is not copied. The opened route exposes only the permitted pointer
fields in a synthetic isolated state. The one-use grant is now consumed.
Current route state is `opened_not_sent`. Final state differs from initial state
only by the recorded synthetic opening. No contact record, send receipt,
delivery record, acknowledgment, content-copy record, intake record, candidate
record, or owner effect exists.

## Existing-Owner Fit

Immutable record `SYNTH-DESTINATION-ACCEPTANCE-IMMEDIATE-003@r1` resolves
better-existing-owner fit to `synthetic-destination-owner`. It does not open
the route, authorize contact or sending, acknowledge receipt, copy native
content, execute intake, accept a candidate, choose disposition, or authorize
external action. Existing-owner fit defeats admission.

## Evidence, Affected Parties, And Legitimacy

Signers and authorities define legitimacy.
`SYNTH-CONDITION-IMMEDIATE-REQUIRED-006@r1` is signed by
`synthetic-condition-owner` under
`synthetic-condition-comparison-authority@r1`.
`SYNTH-ROUTE-SELECTION-IMMEDIATE-004@r1` is signed by
`synthetic-route-selection-owner` under
`synthetic-route-selection-authority@r1`.
`SYNTH-DESTINATION-ACCEPTANCE-IMMEDIATE-003@r1` is signed by
`synthetic-destination-owner` under
`synthetic-destination-pointer-intake-acceptance@r1`.
`SYNTH-ROUTE-OPENING-AUTHORIZATION-002@r1` is signed by
`synthetic-route-opening-authority-owner` under
`synthetic-route-opening-decision-authority@r1`. Evidence pointer is
`synthetic://map-evidence/SYNTH-OPENED-ROUTE-NO-DELIVERY-001@r1`; authority
boundary is
`synthetic://execution-boundary/SYNTH-OPENED-ROUTE-NO-DELIVERY-001@r1`.
Correction owner is `synthetic-mechanism-map-owner`. No real person, owner,
data, system, or external action is involved.

## Comparison, Selection, Acceptance, And Authorization Boundary

Immutable record `SYNTH-CONDITION-IMMEDIATE-REQUIRED-006@r1` matches the
frozen source and makes the immediate route dominate the verified route only
on required timing. Its exact scope supplies no selection, acceptance,
route-opening, contact, admission, disposition, or execution authority.
Immutable record `SYNTH-ROUTE-SELECTION-IMMEDIATE-004@r1` selects
`SYNTH-ALT-OPENED-IMMEDIATE-001@r1` for one prepared pointer-only handoff and
rejects no alternative. It supplies no acceptance, route-opening,
content-copy, contact, admission, disposition, or execution authority.
Immutable record `SYNTH-DESTINATION-ACCEPTANCE-IMMEDIATE-003@r1` accepts the
selected immutable pointer and checksum for one owner-controlled intake after
separately authorized route opening. Immutable record
`SYNTH-ROUTE-OPENING-AUTHORIZATION-002@r1` authorizes
`synthetic-route-opening-actor` to open
`SYNTH-ALT-OPENED-IMMEDIATE-001@r1` exactly once, exposing only the selected
immutable pointer, checksum, correction owner, stop, and acknowledgment state,
after verifying current source, pointer, checksum, custody, selection, and
acceptance matches. It authorizes no contact message, sending event, delivery,
acknowledgment, native-content copy, intake execution, candidate acceptance,
disposition, or external action.

## Route-Opening Trace And Downstream Nonperformance

At `2026-08-27T14:14:44Z`, `synthetic-route-opening-actor` performed the six
required current-record checks; each matched. Immutable trace
`SYNTH-ROUTE-OPENING-TRACE-001@r1` and receipt
`SYNTH-ROUTE-OPENING-RECEIPT-001@r1` record exactly one opening of
`SYNTH-ALT-OPENED-IMMEDIATE-001@r1` under
`SYNTH-ROUTE-OPENING-AUTHORIZATION-002@r1`. The receipt content identifier is
`synthetic-content:route-opening-receipt-opened-no-delivery-001-r1`; its
checksum is valid. The opening exposed only the permitted pointer fields in a
synthetic isolated state. The one-use grant is now consumed. Current route
state is `opened_not_sent`. No contact record, send receipt, delivery record,
acknowledgment, content-copy record, intake record, candidate record, or owner
effect exists.

## Prepared Return And Recovery

Supported prepared return remains `route_to_existing_owner`, but it remains
non-operative without separately authorized sending and a valid delivery or
intake record. Stops trigger on any source, pointer, checksum, custody,
standing, comparison, selection, acceptance, authorization, actor, trace,
receipt, contact, sending, delivery, acknowledgment, route, intake, authority,
or risk revision. Failure modes are opening treated as contact, route state
treated as delivery, acceptance treated as acknowledgment, owner fit treated
as admission, consumed authority reused, and intake invented without a
receipt. Recovery closes the opened synthetic route, preserves every record
and alternative, marks the grant consumed, restores the prepared route to
acceptance-only state, and performs no sending or intake. Exact next wake is
one separately authorized send plus a valid delivery or intake receipt under
frozen current records, or any revision to source, pointer, checksum, custody,
standing, comparison, selection, acceptance, authorization, actor, trace,
receipt, contact, sending, delivery, acknowledgment, route, intake, authority,
or risk.

## Frontier Verification

Frontier Verification confirms current record matches, the exact one-use
grant, valid trace and receipt, permitted exposed fields, consumed authority,
route state `opened_not_sent`, and the absence of contact, send, delivery,
acknowledgment, and intake evidence. It confirms owner fit resolves to
`synthetic-destination-owner` without admission, retains dominated alternative
`SYNTH-ALT-OPENED-VERIFIED-001@r1`, and preserves non-operative
`route_to_existing_owner`. `CMD-0001` and external-action state `none` remain
unchanged.
